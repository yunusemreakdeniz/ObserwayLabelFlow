<UserControl x:Class="ObserwayLabelFlow.App.Controls.ToastHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:ObserwayLabelFlow.App.Converters"
             xmlns:vm="clr-namespace:ObserwayLabelFlow.App.ViewModels"
             IsHitTestVisible="True"
             Focusable="False">

    <UserControl.Resources>
        <conv:ToastKindToAccentBrushConverter x:Key="ToastAccent" />
        <conv:ToastKindToGlyphConverter x:Key="ToastGlyph" />
    </UserControl.Resources>

    <ItemsControl x:Name="ToastList">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Vertical" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>

        <ItemsControl.ItemTemplate>
            <DataTemplate DataType="{x:Type vm:ToastViewModel}">
                <Border x:Name="ToastCard"
                        Width="360"
                        MaxWidth="360"
                        Margin="0,0,0,10"
                        CornerRadius="14"
                        Background="{StaticResource CardBrush}"
                        BorderBrush="{StaticResource StrokeBrush}"
                        BorderThickness="1"
                        RenderTransformOrigin="0.5,0.5"
                        Opacity="0">
                    <Border.RenderTransform>
                        <TranslateTransform x:Name="SlideTransform" X="36" />
                    </Border.RenderTransform>

                    <Grid Margin="14,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                CornerRadius="2"
                                Background="{Binding Kind, Converter={StaticResource ToastAccent}}" />

                        <Grid Grid.Column="2"
                              Width="34"
                              Height="34"
                              VerticalAlignment="Top">
                            <Border CornerRadius="17"
                                    Background="{Binding Kind, Converter={StaticResource ToastAccent}}"
                                    Opacity="0.18" />
                            <TextBlock Text="{Binding Kind, Converter={StaticResource ToastGlyph}}"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="15"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{Binding Kind, Converter={StaticResource ToastAccent}}" />
                        </Grid>

                        <StackPanel Grid.Column="4" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource Text0Brush}"
                                       TextWrapping="Wrap" />
                            <TextBlock Margin="0,4,0,0"
                                       Text="{Binding Message}"
                                       FontSize="13"
                                       LineHeight="19"
                                       Foreground="{StaticResource Text1Brush}"
                                       TextWrapping="Wrap" />
                        </StackPanel>

                        <Button Grid.Column="6"
                                Style="{StaticResource ButtonGhost}"
                                Width="28"
                                Height="28"
                                Padding="0"
                                MinWidth="28"
                                VerticalAlignment="Top"
                                Command="{Binding DismissCommand}"
                                ToolTip="{DynamicResource Loc_ToastDismiss}"
                                Content="✕"
                                FontSize="12"
                                Opacity="0.75" />
                    </Grid>
                </Border>

                <DataTemplate.Triggers>
                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="ToastCard"
                                                 Storyboard.TargetProperty="Opacity"
                                                 From="0"
                                                 To="1"
                                                 Duration="0:0:0.28">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="SlideTransform"
                                                 Storyboard.TargetProperty="X"
                                                 From="36"
                                                 To="0"
                                                 Duration="0:0:0.32">
                                    <DoubleAnimation.EasingFunction>
                                        <BackEase EasingMode="EaseOut" Amplitude="0.35" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </DataTemplate.Triggers>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
